import { LightningElement } from 'lwc';

export default class ParentCmp extends LightningElement {
    /* userDetail ={
        Name : 'Shivang Gupta',
        Age : 30,
    }

    users = ["John", "Kill", "Mark"];
    get firstUser(){
        return this.users[0];
    } */
    age = 30;
}