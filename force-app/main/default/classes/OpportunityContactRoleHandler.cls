public with sharing class OpportunityContactRoleHandler {
    
    /* Trigger 37 : On Insertion of Opportunity Contact ROle if Opportunity is closed show error or if contact role already present then show error */
    public static void showErrorOnInsertionOfContactRole(List<OpportunityContactRole> contactRoleList){
        
    }
}